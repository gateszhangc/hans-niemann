{
  "name": "hans-niemann",
  "version": "1.0.0",
  "private": true,
  "description": "Static keyword site for Hans Niemann",
  "scripts": {
    "sync:public-env": "python3 scripts/sync_public_env.py .env.production assets/public-env.js",
    "build": "npm run sync:public-env",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
